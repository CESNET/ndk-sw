.. _ndp_loopback:

ndp-loopback
------------

Read packets from RX queue and send them back to TX queue.

There is no special configuration for this module.

.. note::
   Currently the output queue can't be specified. Tool uses the same index as for RX queue.
